#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main- check the code
 * @argc: count of arguments supplied
 * @argv: arguments passed
 *
 * Return: always 0
 */
int main(int argc, char **argv)
{
puts(*argv);
return(0);
}

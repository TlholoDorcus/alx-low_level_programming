#include <stdio.h>
#include <math.h>

/**
 * _pow_recursion : brings x raised to y
 * @x: parameter string
 * @y: power
 *
 * Return: 1 for success
 */
int _pow_recursion(int x, int y)
{
double pow(double x, double y);

if (y < 0)
return (-1);

return (x);
}

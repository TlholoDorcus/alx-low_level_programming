#include <stdio.h>
#include <string.h>

/**
 * _puts_recursion - prints a string and a new line
 * @c: parameter string
 *
 * return: 1 for success
 */
int _putchar(char c);
void _puts_recursion(char *s)
{
puts(s);
return;
}

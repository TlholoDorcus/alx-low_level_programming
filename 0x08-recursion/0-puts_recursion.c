#include <stdio.h>
#include <string.h>

/**
 * _puts_recursion - prints a string and a new line
 * @c: parameter string
 *
 * return: a value called c
 */
int _putchar(char c);
void _puts_recursion(char *s)
{
puts(s);
return;
}

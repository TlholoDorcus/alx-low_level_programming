#include <stdio.h>
#include <string.h>

/**
 * _puts_recursion - prints a string and a new line
 * @s: parameter string
 *
 * return: nothing
 *
 */
void _puts_recursion(char *s)
{
puts(s);
return;
}

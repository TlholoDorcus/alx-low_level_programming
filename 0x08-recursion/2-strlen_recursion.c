#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion- gives the length of a string
 * @s: string parameter
 *
 * Return: i
 */
int _strlen_recursion(char *s)
{
int i;
i = strlen(s);
return (i);
}

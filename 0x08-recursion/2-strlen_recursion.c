#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - prints length of string
 * @s: parameter string
 *
 * return: len
 *
 */
int _strlen_recursion(char *s)
{
int len = strlen(s);

return (len);
}

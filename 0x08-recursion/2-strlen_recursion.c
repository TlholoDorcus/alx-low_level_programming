#include <stdio.h>
#include <string.h>

/**
 * int _strlen_recursion - prints length of string
 * @s: parameter string
 *
 * return: length of string
 *
 */
int _strlen_recursion(char *s)
{
int len = strlen(s);

return(len);
}

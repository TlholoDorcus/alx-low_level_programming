#include <stdio.h>
#include <string.h>

/**
 * _strcmp- compares two strings
 * @s1: string passed in
 * @s2: string passed in
 *
 * Return: i for comparisons
 */
int _strcmp(char *s1, char *s2)
{
int i = strcmp(s1, s2);
return (i);
}

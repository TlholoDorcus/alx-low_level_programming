#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - gets the absolute value of n
 * @n: check parameter n
 *
 * Return: absolute value of integer.
 */
int _abs(int n)
{
int x = abs(n);

return (x);
}

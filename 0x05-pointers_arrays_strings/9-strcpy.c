#include <stdio.h>
#include <string.h>

/**
 * *_strcpy - copies stings ponted to by src
 * @dest: string passed in
 * @src: string passed in
 *
 * Return: dest - the pointer
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
